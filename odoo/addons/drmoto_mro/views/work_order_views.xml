<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_drmoto_work_order_tree" model="ir.ui.view">
        <field name="name">drmoto.work.order.list</field>
        <field name="model">drmoto.work.order</field>
        <field name="arch" type="xml">
            <list string="Work Orders" decoration-info="state == 'draft'" decoration-muted="state == 'cancel'" decoration-success="state == 'done'">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="vehicle_plate"/>
                <field name="procedure_id" optional="show"/>
                <field name="date_planned"/>
                <field name="amount_total" widget="monetary"/>
                <field name="state" widget="badge" decoration-success="state == 'done'" decoration-warning="state == 'in_progress'"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_drmoto_work_order_form" model="ir.ui.view">
        <field name="name">drmoto.work.order.form</field>
        <field name="model">drmoto.work.order</field>
        <field name="arch" type="xml">
            <form string="Work Order">
                <header>
                    <button name="action_confirm" string="Confirm" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_start_diagnosis" string="Start Diagnosis" type="object" class="oe_highlight" invisible="state != 'confirmed'"/>
                    <button name="action_quote" string="Create Quote" type="object" class="oe_highlight" invisible="state != 'diagnosing'"/>
                    <button name="action_start_work" string="Start Work" type="object" class="oe_highlight" invisible="state != 'quoted'"/>
                    <button name="action_finish" string="Finish" type="object" class="oe_highlight" invisible="state != 'in_progress'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,in_progress,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="customer_id"/>
                            <field name="vehicle_plate"/>
                            <field name="vehicle_id"/>
                            <field name="procedure_id"/>
                        </group>
                        <group>
                            <field name="date_planned"/>
                            <field name="priority" widget="priority"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Parts &amp; Labor">
                            <field name="line_ids">
                                <list editable="bottom">
                                    <field name="product_id"/>
                                    <field name="name"/>
                                    <field name="quantity"/>
                                    <field name="price_unit"/>
                                    <field name="price_subtotal" widget="monetary"/>
                                </list>
                            </field>
                            <group class="oe_subtotal_footer oe_right">
                                <field name="amount_total" widget="monetary"/>
                            </group>
                        </page>
                        <page string="Description">
                            <field name="description"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>
    
    <!-- Kanban View -->
    <record id="view_drmoto_work_order_kanban" model="ir.ui.view">
        <field name="name">drmoto.work.order.kanban</field>
        <field name="model">drmoto.work.order</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" class="o_kanban_small_column">
                <field name="state"/>
                <field name="name"/>
                <field name="customer_id"/>
                <field name="amount_total"/>
                <field name="currency_id"/>
                <field name="priority"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title"><field name="name"/></strong>
                                    <span class="o_kanban_record_subtitle"><field name="customer_id"/></span>
                                </div>
                                <div class="o_dropdown_kanban dropdown">
                                    <a role="button" class="dropdown-toggle o-no-caret btn" data-toggle="dropdown" href="#" aria-label="Dropdown menu" title="Dropdown menu">
                                        <span class="fa fa-ellipsis-v"/>
                                    </a>
                                    <div class="dropdown-menu" role="menu">
                                        <a role="menuitem" type="edit" class="dropdown-item">Edit</a>
                                        <a role="menuitem" type="delete" class="dropdown-item">Delete</a>
                                    </div>
                                </div>
                            </div>
                            <div class="o_kanban_record_body">
                                <field name="vehicle_plate"/>
                                <div t-if="record.amount_total.value > 0">
                                    <field name="amount_total" widget="monetary"/>
                                </div>
                            </div>
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_left">
                                    <field name="priority" widget="priority"/>
                                </div>
                                <div class="oe_kanban_bottom_right">
                                    <field name="activity_ids" widget="kanban_activity"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_drmoto_work_order_search" model="ir.ui.view">
        <field name="name">drmoto.work.order.search</field>
        <field name="model">drmoto.work.order</field>
        <field name="arch" type="xml">
            <search string="Search Work Orders">
                <field name="name"/>
                <field name="customer_id"/>
                <field name="vehicle_plate"/>
                <filter string="My Orders" name="my_orders" domain="[('create_uid', '=', uid)]"/>
                <filter string="In Progress" name="in_progress" domain="[('state', '=', 'in_progress')]"/>
                <group expand="0" string="Group By">
                    <filter string="Customer" name="group_customer" context="{'group_by': 'customer_id'}"/>
                    <filter string="Status" name="group_status" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_drmoto_work_order" model="ir.actions.act_window">
        <field name="name">Work Orders</field>
        <field name="res_model">drmoto.work.order</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Work Order!
            </p>
        </field>
    </record>

    <!-- Menu Item -->
    <menuitem id="menu_drmoto_work_order"
              name="Work Orders"
              parent="menu_drmoto_root"
              action="action_drmoto_work_order"
              sequence="10"/>
</odoo>
